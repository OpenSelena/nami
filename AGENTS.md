# Agent Guidelines

## Agent skills

### Issue tracker

Issues and specs are tracked via GitHub Issues (`OpenSelena/nami`) using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context repository layout using `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
